@file:Suppress("UnstableApiUsage")

rootProject.name = "cl-plugin-template" // TODO プロジェクト名に変更

dependencyResolutionManagement {
    repositories {
        mavenCentral()
        maven("https://repo.papermc.io/repository/maven-public/")
    }
}
